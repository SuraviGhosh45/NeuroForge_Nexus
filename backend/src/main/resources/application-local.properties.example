# Copy this file to application-local.properties and fill in your local secrets.
# application-local.properties is gitignored and must never be committed.

spring.datasource.password=YOUR_MYSQL_PASSWORD
