spring.application.name=backend
spring.profiles.active=local

# Configure the exact MongoDB Atlas URI in application-local.properties.
spring.data.mongodb.uri=${MONGODB_ATLAS_URI:}
spring.data.mongodb.database=${MONGODB_DATABASE:sdlc_management}
