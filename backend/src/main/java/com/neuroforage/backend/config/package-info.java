/**
 * Reserved for non-security application configuration beans
 * (for example CORS, OpenAPI, or infrastructure settings) as the system grows.
 * Security and password encoding live under the security package.
 */
package com.neuroforage.backend.config;
